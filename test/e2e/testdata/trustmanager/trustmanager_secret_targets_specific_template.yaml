apiVersion: operator.openshift.io/v1alpha1
kind: TrustManager
metadata:
  name: cluster
spec:
  trustManagerConfig:
    trustNamespace: {{.TrustNamespace}}
    secretTargets:
      policy: Specific
      authorizedSecrets:
        - my-secret-1
        - my-secret-2
